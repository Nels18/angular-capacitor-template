import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.angular_capacitor_template.app',
  appName: 'angular-capacitor-template',
  webDir: 'dist',
};

export default config;
